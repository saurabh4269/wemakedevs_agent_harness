# Next

Ordered remaining work. Rewrite when the top item changes. Last rewrite: **2026-08-31 ~19:50 UTC**.

1. **Stand up Heroku from latest `main`** ([heroku.md](heroku.md)): Basic dyno, Postgres 17 + Redis, **port Render Postgres** (`heroku pg:push` / `pg_dump`+`pg_restore`, skip Redis), CNAME `loop` only, optional `import-loop.ts` upsert. Render free web is exhausted; keep Render Postgres until restore verifies. Do not Apply `render.yaml`. Do not put dumps or `HEROKU_API_KEY` in git.
2. **Upload [docs/demo/loop-judge-demo.mp4](demo/loop-judge-demo.mp4) to YouTube.** Conversational cedar voiceover. Do not click Approve until after any re-take. Local backup: `01m1a87xjewncn310ymqy3yz01`.
3. **Publish the Field Report** to `saurabh4269.github.io` as `content/trueforge-harness.mdx`.
4. **Form** when the YouTube URL exists. Answers: [form.md](form.md). Deployed link stays https://loop.heisenbug.in after DNS.

Do not Approve/Deny listed FAIL / film sessions. TypeScript only. Qodo on every substantive PR.
