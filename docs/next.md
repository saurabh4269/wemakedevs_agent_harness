# Next

Ordered remaining work. Rewrite when the top item changes. Last rewrite: **2026-08-31 ~10:20 IST**.

1. **Land this harden PR** (`cursor/hosted-qualify-harden-7d0f`). Qodo `/agentic_review`, Highs 0, merge to `main`. Merge auto-deploys the image. Then re-import LOOP against https://loop.heisenbug.in with the Nemotron `:free` env (see [runbook.md](runbook.md)). Skills: `LOOP_SKILL_REF=main` after merge, or this branch before merge.
2. **Hosted conversion-drop, new session.** Do **not** reuse `01m1advv5np7mqwse1xf2hdpyc` or `01m1ayqn9563da3mgerw6nwpq5`. Seed the exact conversion-drop prompt. Leave Approve sitting if `open_draft_pr` pauses. If Nemotron `:free` flakes again, film the **local** PASS (`01m1a87xjewncn310ymqy3yz01`) and say the judge URL is the same product on hosted TrueForge.
3. **YouTube ≤3 min** of the live user flow: prompt → three named looks → Daytona clone/patch → sitting Approve. Stock TrueForge UI. Say fixtures out loud. Form wants YouTube, not Vimeo. Do not click Approve until after the take.
4. **Publish the Field Report.** Copy [blog.md](blog.md) to `saurabh4269.github.io` as `content/trueforge-harness.mdx` so https://saurabh4269.github.io/blog/trueforge-harness/ stops 404ing.
5. **Form** only when repo + Qodo trail + video exist. Answers live in [form.md](form.md). Team thExplorers. Live URL https://loop.heisenbug.in.

Spark extras (OpenUI + Code Mode) stay inside the same incident. They are already in the spec. Not qualify blockers.

Do not start a Cursor CloudAgent unless Saurabh asks. TypeScript only. Qodo on every substantive PR.
