# Next

Ordered remaining work. Rewrite when the top item changes. Last rewrite: **2026-08-31 ~23:00 IST**.

1. **Stand up Heroku from latest `main`** ([heroku.md](heroku.md)): create app + Postgres + Redis, container push, CNAME `loop` only, re-import Luna. Render free is exhausted. Do not Apply `render.yaml`. Do not put `HEROKU_API_KEY` in git.
2. **Upload [docs/demo/loop-judge-demo.mp4](demo/loop-judge-demo.mp4) to YouTube.** Conversational cedar voiceover. Do not click Approve until after any re-take. Local backup: `01m1a87xjewncn310ymqy3yz01`. Hosted Render sessions will die with the old Postgres.
3. **Publish the Field Report** to `saurabh4269.github.io` as `content/trueforge-harness.mdx`.
4. **Form** when the YouTube URL exists. Answers: [form.md](form.md). Deployed link stays https://loop.heisenbug.in after DNS.

Do not Approve/Deny listed FAIL / film sessions. TypeScript only. Qodo on every substantive PR.
